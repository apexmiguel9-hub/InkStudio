<?xml version="1.0" encoding="utf-8"?>
<!--
  InkAlpha prototype manifest. No local resources: the UI is built
  programmatically and the theme comes from the framework (@android:style).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.inkscape.alpha"
    android:versionCode="1"
    android:versionName="0.1.0-alpha">

    <application
        android:label="InkAlpha"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>